name: Build
on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    outputs:
      code: ${{ github.event_name == 'push' || steps.filter.outputs.code == 'true' }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # ratchet:actions/checkout@v7.0.0
      - uses: dorny/paths-filter@7b450fff21473bca461d4b92ce414b9d0420d706 # ratchet:dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            code:
              - 'src/**'
              - 'examples/**'
              - 'Cargo.toml'
              - 'Cargo.lock'

  build:
    name: Build (${{ matrix.target }})
    needs: [changes]
    if: needs.changes.outputs.code == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
          - target: aarch64-apple-darwin
            os: macos-latest
          - target: x86_64-pc-windows-msvc
            os: windows-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # ratchet:actions/checkout@v7.0.0
      - uses: tylerbutler/actions/setup-rust@c3de10f5c576265f4910698528d949179081f25b # ratchet:tylerbutler/actions/setup-rust@main
        with:
          targets: ${{ matrix.target }}
      - name: Check library compiles
        run: cargo check --target ${{ matrix.target }}
      - name: Check all features
        run: cargo check --all-features --target ${{ matrix.target }}

  # Gate job for required status check — reports a single fixed-name check
  # that passes when all matrix jobs pass (or are skipped).
  build-result:
    name: Build Result
    if: always()
    needs: [build]
    runs-on: ubuntu-latest
    steps:
      - name: Check build matrix result
        run: |
          if [[ "${{ needs.build.result }}" == "failure" || "${{ needs.build.result }}" == "cancelled" ]]; then
            echo "Build matrix failed or was cancelled"
            exit 1
          fi
